/* Public configuration. Never put a client secret, access token or password here. */
window.ICS_CONFIG = {
  googleClientId: '', // ...apps.googleusercontent.com (Web application OAuth client)
  urlReaderEndpoint: '', // https://your-reader.workers.dev/
  maxFileBytes: 10485760,
  maxPdfPages: 50
};
