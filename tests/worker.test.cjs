const {test}=require('node:test'),a=require('node:assert/strict');
test('URL allowlist blocks local hosts, credentials, HTTP and unknown domains',async()=>{const {allowed}=await import('../url-reader/worker.mjs');for(const s of ['http://example.com','https://127.0.0.1','https://localhost','https://user:pass@example.com','https://other.com','https://example.com:8443'])a.throws(()=>allowed(s,['example.com']));a.equal(allowed('https://example.com/event',['example.com']).hostname,'example.com');});
test('worker CORS and unsupported methods',async()=>{const {default:worker}=await import('../url-reader/worker.mjs');const env={ALLOWED_ORIGINS:'https://79mas.github.io'};a.equal((await worker.fetch(new Request('https://reader.test'),env)).status,403);a.equal((await worker.fetch(new Request('https://reader.test',{method:'OPTIONS',headers:{Origin:env.ALLOWED_ORIGINS}}),env)).status,204);});
